# @damm/contracts

Re-exporta tipos públicos de `@damm/copilot` y `@damm/optimizer-route` para que frontend y backend dependan de un único punto de entrada, sin lógica de negocio.

Uso:

```ts
import type { CopilotQuestionInput, InputPayload } from "@damm/contracts";
```
