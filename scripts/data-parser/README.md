# data-parser

Placeholders para scripts de normalización (Excel/PDF → JSON).  
Durante el hackathon puedes añadir aquí scripts en Node o Python; mantenlos fuera de `apps/` y `packages/` para no mezclar con la API publicada.
